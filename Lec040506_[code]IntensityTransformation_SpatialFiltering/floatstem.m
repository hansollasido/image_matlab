function floatstem(x,y,Bias,H,Z,L,F,C) %  floatstem(x,y,bias,H,Z,L,C) returns the stem type graph y+Bias starting%  stem lines from the level of Bias (nt zero y-axis). %  (Note): stem(x,y) produces stem lines starting from 0-level y axis.%  x is x-coordinate of x-axis.%  y should be real number vector of same size as x, size(x) = size(y) and %  y is classified into three classes and assign three kinds of marks H,Z,%  and L, for example,%      H:  '^'  for y>= ep, %      Z:  'o'  for |y|<ep,%      L:  'V'  for y <=-ep. %  where ep is a suitable small positive number.%  F indicates the shape of stem whether it is filled or not such that%    if F is 'filled', fill the stems and else  make empty.%  C denotes color of markers 'r', 'g',..., etc. %%  Copyright 2010 T.Y. Choi.%  The above 'floatstem' is just renamed 'ss_floatstem', 108 Topics for %  Signals and Systems with MATLAB Soutions, A_Jin Publishing, 2010.%  Publisher web site: http://www.ajin.to/if nargin<8    error('Missing variable? ');end   yB = y+Bias;  ep = max(abs(y))/1000;  % small numberif strcmp(F,'filled') ==1 for k = 1: length(x),    plot([x(k),x(k)], [Bias, y(k)+Bias] ); hold on;    if y(k)>ep            % Positve       %scatter(x(k),yB(k), H,F,C); hold on;       scatter(x(k),yB(k),F,C); hold on;    elseif abs(y(k))<=ep  % Close to zero       %scatter(x(k),yB(k), Z,F,C); hold on;       scatter(x(k),yB(k),F,C); hold on;    else                  % Negative       %scatter(x(k),yB(k), L,F,C); hold on;       scatter(x(k),yB(k), F,C); hold on;    endendelse  for k = 1: length(x),    plot([x(k),x(k)], [Bias, y(k)+Bias] ); hold on;    if y(k)>ep       scatter(x(k),yB(k), H,C); hold on;    elseif abs(y(k))<=ep       scatter(x(k),yB(k), Z,C); hold on;    else       scatter(x(k),yB(k), L,C); hold on;    end  end end